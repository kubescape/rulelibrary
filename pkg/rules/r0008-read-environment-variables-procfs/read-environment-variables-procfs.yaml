apiVersion: kubescape.io/v1
kind: Rules
metadata:
  name: read-environment-variables-procfs-rule
  namespace: kubescape
  labels:
    app: kubescape
spec:
  rules:
  - name: "Read Environment Variables from procfs"
    enabled: true
    id: "R0008"
    description: "Detecting reading environment variables from procfs."
    expressions:
      message: "'Reading environment variables from procfs: ' + event.fullPath + ' by process ' + event.comm"
      unique_id: "event.comm + '_' + event.fullPath"
      rule_expression:
        - event_type: "open"
          expression: >
            event.fullPath.startsWith('/proc/') && 
            event.fullPath.endsWith('/environ') &&
            !ap.was_path_opened_with_suffix(event.containerId, '/environ')
    profile_dependency: 0 # Required
    severity: 5 # Medium
    support_policy: false
    tags:
      - "anomaly"
      - "procfs"
      - "environment"
      - "applicationprofile"
