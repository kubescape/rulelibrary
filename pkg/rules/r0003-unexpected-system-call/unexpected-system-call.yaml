apiVersion: kubescape.io/v1
kind: Rules
metadata:
  name: unexpected-system-call-rule
  namespace: kubescape
  labels:
    app: kubescape
spec:
  rules:
    - name: "Syscalls Anomalies in container"
      enabled: true
      id: "R0003"
      description: "Detects unexpected system calls that are not whitelisted by application profile"
      expressions:
        message: "'Unexpected system call detected: todo with PID ' + string(event.pid)"
        uniqueId: "'todo'"
        ruleExpression:
          - eventType: "syscall"
            expression: "!ap.were_syscalls_used(event.containerId, event.syscalls)"
      profileDependency: 0
      severity: 1
      supportPolicy: false
      tags:
        - "anomaly"
        - "syscall"
        - "applicationprofile"
