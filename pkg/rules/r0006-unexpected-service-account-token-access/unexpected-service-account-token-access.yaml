apiVersion: kubescape.io/v1
kind: Rules
metadata:
  name: unexpected-service-account-token-access-rule
  namespace: kubescape
  labels:
    app: kubescape
spec:
  rules:
  - name: "Unexpected Service Account Token Access"
    enabled: true
    id: "R0006"
    description: "Detecting unexpected access to service account token."
    expressions:
      message: "'Unexpected access to service account token: ' + data.event.fullPath + ' with flags: ' + data.event.flags.join(',')"
      unique_id: "data.event.comm"
      rule_expression:
        - event_type: "open"
          expression: >
            ((data.event.fullPath.startsWith('/run/secrets/kubernetes.io/serviceaccount') && data.event.fullPath.endsWith('/token')) || 
             (data.event.fullPath.startsWith('/var/run/secrets/kubernetes.io/serviceaccount') && data.event.fullPath.endsWith('/token')) ||
             (data.event.fullPath.startsWith('/run/secrets/eks.amazonaws.com/serviceaccount') && data.event.fullPath.endsWith('/token')) ||
             (data.event.fullPath.startsWith('/var/run/secrets/eks.amazonaws.com/serviceaccount') && data.event.fullPath.endsWith('/token'))) &&
            !ap.was_path_opened_with_prefix(data.event.runtime.containerId, '/run/secrets/kubernetes.io/serviceaccount') &&
            !ap.was_path_opened_with_prefix(data.event.runtime.containerId, '/var/run/secrets/kubernetes.io/serviceaccount') &&
            !ap.was_path_opened_with_prefix(data.event.runtime.containerId, '/run/secrets/eks.amazonaws.com/serviceaccount') &&
            !ap.was_path_opened_with_prefix(data.event.runtime.containerId, '/var/run/secrets/eks.amazonaws.com/serviceaccount')
    profile_dependency: 1
    severity: 5
    support_policy: false
    tags:
      - "anomaly"
      - "serviceaccount"
      - "applicationprofile" 